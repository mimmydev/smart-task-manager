meta {
  name: Create Task [Success]
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/tasks
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "userId": "{{USER_ID}}",
    "title": "Bruno API testing task"
  }
  // Description: Test successful task creation
}
